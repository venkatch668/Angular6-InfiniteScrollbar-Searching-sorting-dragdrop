import { CartListInfiniteScrollerDirective } from './cart-list-infinite-scroller.directive';

describe('CartListInfiniteScrollerDirective', () => {
  it('should create an instance', () => {
    const directive = new CartListInfiniteScrollerDirective();
    expect(directive).toBeTruthy();
  });
});
